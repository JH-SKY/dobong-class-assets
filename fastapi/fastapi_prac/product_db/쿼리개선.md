GET "/products"  
    - category를 같이 가져오도록 수정

GET "/products/{id}"  
    - category를 같이 가져오도록 수정

GET "/users/{user_id}/wishlist"
    - wishlist , product를 같이 가져오도록 수정

GET "/users/{user_id}/wishlist"
    - repository 로직에 join이 있지만 우리에게 전달되는 데이터는 product 뿐입니다.
    따라서 n+1 문제가 발생하지 않습니다.
    join은 필터링 / 정렬을 위해서만 사용됩니다.